[package]
name = "rip_git"
version = "0.1.0"
edition = "2024"

[workspace]
members = [".", "templating", "migration", "entity", "server"]

[dependencies]
# bewerbungs_tool_api = { path = "api" }
migration = { path = "migration" }
templating = { path = "templating" }
entity = { path = "entity" }
server = { path = "server" }
dotenv = "0.15.0"
