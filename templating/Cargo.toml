[package]
name = "templating"
version = "0.1.0"
edition = "2024"

[features]
axum = ["dep:axum"]

[dependencies]
templating_macro = { path = "../templating_macro" }
axum = { version = "0.8.9", optional = true }
