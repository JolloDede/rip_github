# rip_templating_macro (Internal Implementation)

> ⚠️ **Warning:** This is an internal macro crate for **`rip_templating`**. 
> You should **not** depend on this crate directly in your project. 

Please use the main [rip_templating](https://crates.io/crates/rip_templating) library instead, which automatically includes and re-exports everything you need.
